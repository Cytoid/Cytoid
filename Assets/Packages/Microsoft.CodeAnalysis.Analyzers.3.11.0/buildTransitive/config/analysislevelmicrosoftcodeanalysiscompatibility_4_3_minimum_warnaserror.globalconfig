# NOTE: Requires **VS2019 16.7** or later

# 'MicrosoftCodeAnalysisCompatibility' Rules from '4.3' release with 'Minimum' analysis mode escalated to 'error' severity
# Description: 'MicrosoftCodeAnalysisCompatibility' Rules with enabled-by-default state from '4.3' release with 'Minimum' analysis mode. Rules that are first released in a version later than '4.3' are disabled. Enabled rules with 'warning' severity are escalated to 'error' severity to respect 'CodeAnalysisTreatWarningsAsErrors' MSBuild property.

is_global = true

global_level = -99

