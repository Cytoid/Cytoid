{
  "_comment": "Round-trip expectation for session_result_payload.tier_retry.valid.json: parser must succeed; fromJson(toJson(x)) must be structurally equal to fromJson(input).",
  "throws": false
}
