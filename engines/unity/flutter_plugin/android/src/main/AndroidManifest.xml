<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">
    <application android:extractNativeLibs="true">
        <!--
          The activity class name MUST match `CytoidNativeConfig.UNITY_GAMEPLAY_ACTIVITY`
          in `src/main/kotlin/org/cytoid/gamecore/CytoidNativeConfig.kt`. AAPT requires
          a literal here; the Kotlin constant is the single source of truth and this
          value is verified by `CytoidNativeConfigTest`.
        -->
        <activity
            android:name="me.tigerhix.cytoid.CytoidPluginActivity"
            android:excludeFromRecents="true"
            android:exported="false"
            android:label="@string/cytoid_game_core_activity_label"
            android:launchMode="singleTop"
            android:screenOrientation="landscape"
            android:taskAffinity=""
            tools:node="merge"
            tools:replace="android:excludeFromRecents,android:exported,android:label,android:launchMode,android:screenOrientation,android:taskAffinity">
            <intent-filter tools:node="removeAll" />
        </activity>
    </application>
</manifest>
