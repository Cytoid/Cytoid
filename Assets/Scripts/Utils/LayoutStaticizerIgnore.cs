using UnityEngine;

// Mark a GameObject's layout group and content size fitter as editor-only, that is,
// they should never be activated by LayoutStaticizer.Activate().
public class LayoutStaticizerIgnore : MonoBehaviour
{
}